
\newcommand{\figureTermSyntaxContent}{%
\[
  \arraycolsep=0pt
  \begin{array}{lrlll}
    q & {}\in Pat & {}::={} &
      \pVar{\backrefs}{u}{x}
      \mid \conflictHole{q, \ldots, q}
      \mid \multiVertex{u}
      \mid \cycleVertex{u}
      \mid \hole
      \\
    \tau & {}\in Typ & {}::={} &
      \tArrow{\backrefs}{u}{\tau}{\tau}
      \mid \tNum{\backrefs}{u}
      \mid \conflictHole{u, \ldots, u}
      \mid \multiVertex{u}
      \mid \cycleVertex{u}
      \mid \hole
      \\
    e & {}\in Exp & {}::={} &
      \eVar{\backrefs}{u}{x}
      \mid \eFun{\backrefs}{u}{q}{\tau}{e}
      \mid \eApp{\backrefs}{u}{e}{e}
      \mid \eNum{\backrefs}{u}{n}
      \mid \ePlus{\backrefs}{u}{e}{e}
      \mid \eTimes{\backrefs}{u}{e}{e}
      \mid \conflictHole{e, \ldots, e}
      \mid \multiVertex{u}
      \mid \cycleVertex{u}
      \mid \hole
      \\
  \end{array}
\]%
}

\newcommand{\figureTermSyntax}{%
\begin{figure}
  \figureTermSyntaxContent
  \caption{Base syntax, as a grammar, for the lambda calculus that we are considering.}
  \label{fig:Base syntax}
\end{figure}%
}