
\newcommand{\figureTermSyntaxContent}{%
\[
  \arraycolsep=0pt
  \begin{array}{lcllll}
    t & {}\in{} & Term & {}::={} &
      e
      \mid \tau
      \mid q
    \\
    e & {}\in{} & Exp & {}::={} &
      \eVar{G}{x}
      \mid \eFun{G}{q}{\tau}{e}
      \mid \eApp{G}{e_1}{e_2}
      \mid \eNum{G}{n}
      \mid \ePlus{G}{e_1}{e_2}
      \mid \eTimes{G}{e_1}{e_2}
      \mid \multiVertex{\e}
      \mid \cycleVertex{\e}
      \mid \conflictHole[i < n]{e_i}
      \mid \emptyHole{v}{p}
    \\
    \tau & {}\in{} & Typ & {}::={} &
      \tArrow{G}{\tau_1}{\tau_2}
      \mid \tNum{G}
      \mid \multiVertex{\e}
      \mid \cycleVertex{\e}
      \mid \conflictHole[i < n]{\tau_i}
      \mid \emptyHole{v}{p}
    \\
    q & {}\in{} & Pat{} & {}::={} &
      \pVar{G}{x}
      \mid \multiVertex{\e}
      \mid \cycleVertex{\e}
      \mid \conflictHole[i < n]{q_i}
      \mid \emptyHole{v}{p}
    \\
  \end{array}
\]
}

\newcommand{\figureTermSyntax}{%
\begin{figure}
  \figureTermSyntaxContent
  \caption{Syntax of terms}
  \Description{This figure describes the grammar for the syntax of terms sorted in expressions, types, and pattern variables}
  \label{fig:Syntax}
\end{figure}%
}